 # WHILE = Enquanto

acumulador = 0
num = int (input("informe um número: "))

while num != 0:
    acumulador += num
    num = int (input("informe um número: "))
print (acumulador)

# acumulador = acumulador + num ou acumulador += num (+=, incrementado)